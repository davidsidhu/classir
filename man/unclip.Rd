% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unclip.R
\name{unclip}
\alias{unclip}
\title{Read data copied from Excel}
\usage{
unclip(
  sep = "\\t",
  header = TRUE,
  check_names = FALSE,
  dt = requireNamespace("data.table", quietly = TRUE),
  quiet = FALSE,
  ...
)
}
\arguments{
\item{sep}{Column separator. Default a tab, which is what Excel puts on the
clipboard.}

\item{header}{Logical. Does the first row hold column names? Default TRUE.}

\item{check_names}{Logical. If TRUE, run column names through
\code{make.names()}, which replaces spaces with dots. Default FALSE.}

\item{dt}{Logical. If TRUE, return a data.table. Defaults to TRUE when
data.table is installed, since that is likely what you want it as.}

\item{quiet}{Logical. If TRUE, don't report what was read.}

\item{...}{Passed to \code{utils::read.table()}.}
}
\value{
A data frame, or a data.table when \code{dt = TRUE}.
}
\description{
Reads whatever is on the clipboard as a table, so a block copied out of
Excel becomes a data frame in one call:

\preformatted{
d <- unclip()
}

The counterpart to \code{\link[=copy_table]{copy_table()}}, which goes the other way.
}
\details{
A few defaults differ from \code{read.table()}, because clipboard data from a
spreadsheet is not the same as a well-behaved text file. Apostrophes are not
treated as quote characters, so a word like \verb{don't} does not break the
parse; \verb{#} is not treated as a comment, so it does not truncate a line; and
empty cells become \code{NA} rather than empty strings.

Column names are left as Excel had them, spaces and all, rather than being
mangled into \code{Word.Count}. Set \code{check_names = TRUE} for R's usual behaviour.

On macOS this uses \code{pbpaste} and on Windows the system clipboard. On Linux
it needs \code{xclip} or \code{xsel} installed.
}
\examples{
\dontrun{
# copy a block in Excel, then
d <- unclip()

# no header row
d <- unclip(header = FALSE)
}

}
