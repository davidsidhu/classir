% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/best_subsets_boot.R
\name{best_subsets_boot}
\alias{best_subsets_boot}
\title{Best-subsets regression with BIC and bootstrap stability}
\usage{
best_subsets_boot(
  data,
  outcome,
  predictors = NULL,
  subset = NULL,
  B = 300,
  nvmax = NULL,
  seed = NULL,
  progress = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame containing the outcome and predictors.}

\item{outcome}{A string giving the name of the outcome variable
(e.g., \code{"Agreeableness"}).}

\item{predictors}{Optional predictor specification. Can be:
\itemize{
\item \code{NULL} (default): use all columns except \code{outcome}
\item character vector: predictor column names
\item numeric vector: predictor column positions
}}

\item{subset}{Optional logical or numeric vector used to subset rows
of \code{data} before fitting models. Logical vectors must have length
\code{nrow(data)}; numeric vectors are treated as row indices.}

\item{B}{Number of bootstrap samples. Default is 300.}

\item{nvmax}{Maximum number of predictors considered by
\code{leaps::regsubsets()}. Default is \code{length(predictors)}.}

\item{seed}{Optional integer seed for reproducibility. If \code{NULL},
the current RNG state is used.}

\item{progress}{Logical; if TRUE (default), show a text progress bar
for the bootstrap iterations.}

\item{...}{Additional arguments passed to \code{leaps::regsubsets()}.}
}
\value{
A list with components:
\describe{
\item{model}{The \code{lm} object refit on the full (possibly subset) data
using the best-BIC model.}
\item{formula}{The \code{formula} used for \code{model}.}
\item{sel_freq}{Named numeric vector of selection frequencies
(between 0 and 1) for each predictor.}
\item{regsubsets}{The original \code{leaps::regsubsets} object.}
\item{summary}{The \code{summary()} of the original regsubsets call.}
}
}
\description{
Runs best-subsets regression using BIC to choose model size,
then uses bootstrapping to estimate how often each predictor
is selected as part of the best-BIC model.
}
\examples{
\dontrun{
# Example: restrict to females and specific columns by index
af <- best_subsets_boot(
  data       = d,
  outcome    = "Agreeableness",
  predictors = 11:44,
  subset     = d$Gender == "Female",
  B          = 300,
  seed       = 123
)

af$formula
summary(af$model)
sort(af$sel_freq, decreasing = TRUE)

# Same but pre-subsetted data and automatic predictors:
d_a <- d[d$Gender == "Female", c(5, 11:44)]
af2 <- best_subsets_boot(
  data    = d_a,
  outcome = "Agreeableness",
  B       = 300,
  seed    = 123
)
}

}
