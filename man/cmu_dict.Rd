% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcribe_phonemes.R
\name{cmu_dict}
\alias{cmu_dict}
\title{Get the CMU pronouncing dictionary, downloading it if needed}
\usage{
cmu_dict(ask = interactive(), refresh = FALSE, stress_sensitive = FALSE)
}
\arguments{
\item{ask}{Logical. If TRUE (the default in interactive sessions), prompt
before downloading. If FALSE, download without prompting.}

\item{refresh}{Logical. If TRUE, re-download and rebuild even if a cached
copy exists. Default FALSE.}

\item{stress_sensitive}{Passed to \code{build_cmu_ipa()}. Cached separately from
the stress-insensitive version.}
}
\value{
A data.frame with columns \code{word} and \code{ipa}.
}
\description{
Returns a word -> IPA lookup table. On first use the CMU pronouncing
dictionary is downloaded and converted, then cached on disk, so the download
happens only once per machine. Later calls read the cached copy, and
repeated calls within a session reuse the version already in memory.
}
