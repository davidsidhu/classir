% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_guided.R
\name{prep_guided}
\alias{prep_guided}
\title{Prepare a dataset for analysis, step by step}
\usage{
prep_guided(d)
}
\arguments{
\item{d}{A data.frame or data.table.}
}
\value{
Invisibly, the prepared data. It is also assigned back to \code{d} in the
calling environment.
}
\description{
A guided version of \code{\link[=prep]{prep()}} for people who would rather answer questions
than write out a function call. It walks through each preparation step in
turn -- dropping incomplete rows, subsetting, setting reference levels,
effects coding, standardising, and dropping unused columns -- and any step
can be skipped by pressing enter.

The prepared data is assigned back to the dataset you passed in, and the
equivalent \code{prep()} call is printed so the step can be pasted into a script.
}
\details{
Renaming columns is not offered here; use \code{\link[=prep]{prep()}} directly for that.

The function is interactive by design and stops with an explanatory error if
called from a script or an R Markdown document.
}
\examples{
\dontrun{
prep_guided(demo_words)
}

}
