% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/converge.R
\name{converge}
\alias{converge}
\title{Get a non-converging lmer model to converge}
\usage{
converge(
  m,
  data = NULL,
  optimizer = "bobyqa",
  maxfun = 2e+05,
  singular_fails = TRUE,
  var_method = c("min", "sum"),
  restore = TRUE,
  max_drops = 20,
  p_values = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{m}{A fitted \code{lmerMod} (or \code{lmerModLmerTest}) object.}

\item{data}{The data the model was fitted to. If \code{NULL} (default), it is
recovered from the model call.}

\item{optimizer}{Optimizer to try at step 1. Default \code{"bobyqa"}.}

\item{maxfun}{Iteration limit to try at step 1. Default 2e5.}

\item{singular_fails}{Logical. If TRUE (default), a singular fit counts as a
failure and simplification continues.}

\item{var_method}{How to summarise variance for a term that occupies several
rows in \code{VarCorr()} -- a factor with more than two levels, whose contrast
columns each get their own variance. \code{"min"} (default) judges the factor by
its lowest-variance slope; \code{"sum"} adds them.}

\item{restore}{Logical. If TRUE (default), try to undo the last remedies
once the model converges.}

\item{max_drops}{Maximum number of random slopes to drop before giving up.
Default 20.}

\item{p_values}{Logical. If TRUE (default), convert the final model with
\code{lmerTest::as_lmerModLmerTest()} so that \code{summary()} reports Satterthwaite
degrees of freedom and p-values for the fixed effects.}

\item{verbose}{Logical. If TRUE (default), report each step.}

\item{return_log}{Logical. If TRUE, return a list of \code{model} and \code{log}
rather than just the model. Default FALSE.}
}
\value{
The converged model, or the last model tried if nothing worked. With
\code{return_log = TRUE}, a list with \code{model} and \code{log}.
}
\description{
Works through a fixed sequence of remedies for a mixed model that failed to
converge, stopping as soon as one works:
\enumerate{
\item Refit with a different optimizer and more iterations.
\item Drop the correlations between random intercepts and slopes (\code{||}), which
requires refitting through \code{afex::lmer_alt()}.
\item Drop random slopes one at a time -- highest-order term first, and among
those the one with the smallest variance -- refitting after each.
}

This is the approach reported in Sidhu et al. (2026).

Sidhu, D. M., Parajuli, P., & Muraki, E. J. (2026). That sounds exciting!
Emotional iconicity facilitates processing and recall. \emph{Journal of
Memory and Language}, \emph{150}, 104769.

Once a model converges, \code{restore = TRUE} tries to walk back the last two
steps: first putting the correlations back, then returning to the original
optimizer settings, keeping each only if convergence survives.

As with any automated analysis, this is a tool that should be used along with
careful thought. Please don't rely on this without understanding the steps
being taken and their purpose.

Only \code{lmer} models are handled. \code{converge()} stops on a \code{glmer} model rather
than simplifying it: convergence trouble in a generalized model often comes
from separation or predictor scaling, which pruning random slopes will not
fix.
}
\examples{
\dontrun{
m1 <- lmer(RT ~ Iconicity * Freq + (1 + Iconicity | Subject) +
                                   (1 + Freq | Item), data = d)
m2 <- converge(m1)
}

}
