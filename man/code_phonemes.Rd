% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcribe_phonemes.R
\name{code_phonemes}
\alias{code_phonemes}
\title{Code the presence or count of each phoneme}
\usage{
code_phonemes(ipa, inventory = NULL, counts = TRUE, prefix = "ph_", ...)
}
\arguments{
\item{ipa}{Character vector of IPA strings. \code{NA} entries produce a row of
\code{NA}s.}

\item{inventory}{Optional character vector of phonemes to use as columns. If
\code{NULL} (default), every segment observed in \code{ipa} is used -- convenient
for a single dataset, but see \code{english_phonemes} when columns need to
match across datasets.}

\item{counts}{Logical. If TRUE (default), each cell is the number of times
the phoneme occurs. If FALSE, cells are 0/1 presence indicators.}

\item{prefix}{Prefix for the new column names. Default \code{"ph_"}.}

\item{...}{Passed to \code{ipa_to_segments()}, e.g. \code{extra_multichar} or
\code{keep_stress}.}
}
\value{
A data.frame with one row per element of \code{ipa} and one column per
phoneme in \code{inventory}.
}
\description{
Segments IPA strings (keeping affricates and diphthongs intact) and returns
one column per phoneme.
}
