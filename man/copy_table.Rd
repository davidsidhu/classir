% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copy_table.R
\name{copy_table}
\alias{copy_table}
\title{Copy a table to the clipboard, ready to paste into Excel}
\usage{
copy_table(
  x,
  digits = 3,
  p_nice = TRUE,
  names_nice = TRUE,
  rename = NULL,
  row_order = NULL,
  drop_cols = NULL,
  file = NULL,
  row_name = "Term",
  quiet = FALSE
)
}
\arguments{
\item{x}{A model, an anova table, a data frame or a matrix.}

\item{digits}{Decimal places. Default 3.}

\item{p_nice}{Logical. If TRUE (default), format p-values with stars, as
\verb{< .001} where they are too small to show, and without a leading zero.}

\item{names_nice}{Logical. If TRUE (default), tidy the term names.}

\item{rename}{Named character vector relabelling terms, old name on the
left, e.g. \code{c(PoS = "Part of Speech")}. Applied after \code{names_nice}.}

\item{row_order}{Character vector putting the rows in the order you want,
named as they are in the model rather than as \code{rename} relabels them. Terms
you leave out keep their existing order at the end.}

\item{drop_cols}{Character vector of columns to leave out, e.g.
\code{c("t value", "df")}.}

\item{file}{Optional path to write the table to. \code{.csv} is written
comma-separated, anything else tab-separated.}

\item{row_name}{Name for the column holding the row names. Default \code{"Term"}.
\code{NULL} leaves row names out.}

\item{quiet}{Logical. If TRUE, don't print the table that was copied.}
}
\value{
Invisibly, the formatted data frame.
}
\description{
Takes model output -- or any table -- and puts it on the clipboard as
tab-separated text, so pasting into Excel drops each value into its own cell
without going through Text to Columns. Numbers are rounded and written in
plain notation, so nothing arrives as \code{1.2e-16}, and row names become a first
column so predictor names come across too.

\code{p_nice = TRUE}, the default, formats p-value columns the way a paper would:
significance stars, anything below the rounding threshold as \verb{< .001}, and no
zero before the decimal point.

\code{names_nice = TRUE}, the default, tidies the term names. Interactions are
joined with a multiplication sign rather than a colon, and a categorical
predictor's terms are written out against their reference level, so
\code{PoSverb} becomes \verb{PoS (Verb vs. Noun)}.

\code{rename} relabels terms, with the old name on the left as elsewhere in the
package: \code{rename = c(PoS = "Part of Speech")}.

\code{row_order} puts the rows in the order you want them, and \code{drop_cols}
removes columns you do not report. \code{file} writes the table out instead of
only copying it.
}
\details{
Works with \code{lm}, \code{glm}, \code{lmer} and \code{lmerTest} models, \code{anova} tables, and
anything that is already a data frame or matrix. For a model, the fixed
effects table is used -- the same thing \code{summary()} prints.

On macOS this uses \code{pbcopy} and on Windows the system clipboard. On Linux it
needs \code{xclip} or \code{xsel}; without one, the text is printed to copy by hand.
}
\examples{
\dontrun{
m <- lm(iconicity ~ freq * pos, data = demo_words)
copy_table(m)

copy_table(m, row_order = c("freq", "pos"),
           drop_cols = "t value",
           rename = c(freq = "Frequency", pos = "Part of Speech"))

copy_table(m, file = "table1.csv")
}

}
