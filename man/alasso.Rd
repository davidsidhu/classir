% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alasso.R
\name{alasso}
\alias{alasso}
\title{Adaptive lasso}
\usage{
alasso(
  d,
  dv,
  predictors,
  lambda = c("1se", "min"),
  n_cv = 11,
  nfolds = 10,
  seed = NULL,
  scale = TRUE,
  r2 = FALSE,
  refit = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{d}{A data.frame or data.table.}

\item{dv}{Character: name of the outcome column.}

\item{predictors}{Character or numeric: the predictor columns.}

\item{lambda}{Which lambda to use, \code{"1se"} (default) or \code{"min"}.}

\item{n_cv}{Number of cross-validation runs to take the median lambda over.
Default 11.}

\item{nfolds}{Folds per cross-validation run. Default 10.}

\item{seed}{Optional integer seed, for reproducible fold assignment.}

\item{scale}{Logical. If TRUE (default), standardise numeric predictors.}

\item{r2}{Logical. If TRUE, report the R-squared of the lasso predictions.
Default FALSE.}

\item{refit}{Logical. If TRUE, also fit an ordinary \code{lm()} using the
selected predictors. Default FALSE. See the note below on inference.}

\item{quiet}{Logical. If TRUE, suppress printed output. Default FALSE.}
}
\value{
Invisibly, a list with \code{coefficients}, \code{selected}, \code{dropped},
\code{lambda}, \code{data} (the complete-case, scaled data used), \code{fit} (the
\code{cv.glmnet} object), and, when requested, \code{r2} and \code{lm}.
}
\description{
Runs an adaptive lasso: a ridge regression supplies weights
(\verb{penalty.factor = 1 / |ridge coefficient|}), and a lasso is then fitted
with those weights, so predictors with small ridge coefficients are
penalised harder. Cross-validation is repeated \code{n_cv} times and the median
lambda is used, which stabilises selection against the randomness of the CV
folds.

See Sidhu et al. (2021) for a description of this method.

Sidhu, D. M., Westbury, C., Hollis, G., & Pexman, P. M. (2021). Sound
symbolism shapes the English language: The maluma/takete effect in English
nouns. \emph{Psychonomic Bulletin & Review}, \emph{28}, 1390-1398.

Rows missing values on the outcome or any predictor are dropped before
fitting. Numeric predictors are standardised by default so the coefficients
are comparable.

\code{refit = TRUE} fits an ordinary regression using the selected predictors.
}
\section{Inference after selection}{

If \code{refit = TRUE}, the p-values from the \code{lm()} do not account for the fact
that the predictors were chosen by the lasso on the same data. They are
anticonservative and should not be reported as ordinary p-values. The
coefficients and R-squared are fine.
}

\examples{
\dontrun{
alasso(d, "P_Iconicity", c("Freq_SUBTLEXUS", "AoA_Kuper", "Conc_Brys"))
alasso(d, "P_Iconicity", pred_vars, lambda = "min", r2 = TRUE, seed = 999)
}

}
