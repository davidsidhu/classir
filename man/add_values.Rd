% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_values.R
\name{add_values}
\alias{add_values}
\title{Add values from a lookup dataset by id}
\usage{
add_values(
  d,
  id,
  lookup,
  lookup_id,
  lookup_cols,
  add_all = FALSE,
  new_cols = NULL,
  add_subset,
  multiple = c("first", "all", "error"),
  fix_types = FALSE,
  complete_only = FALSE,
  ignore_case = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{d}{A data.frame/data.table, or a character vector of ids.}

\item{id}{Character: name of the id column in \code{d}. Defaults to the leftmost
column, with a message.}

\item{lookup}{Either a data.frame or a path to a file to read
(.csv, .tsv/.txt, .rds, .xlsx/.xls).}

\item{lookup_id}{Character: name of the id column in \code{lookup}. Defaults to
\code{id} if present, otherwise the leftmost column, with a message.}

\item{lookup_cols}{Character vector: names of the columns in \code{lookup} to add.
Not needed when \code{add_all = TRUE}.}

\item{add_all}{Logical. If TRUE, every column in \code{lookup} except \code{lookup_id}
is added, and \code{lookup_cols} is ignored. Default FALSE.}

\item{new_cols}{Optional character vector of names for the added columns.
Either unnamed and the same length as \code{lookup_cols} (matched by position),
or named, where the names are entries in \code{lookup_cols} and the values are
their new names (allowing a subset to be renamed).}

\item{add_subset}{Optional unquoted expression evaluated in \code{d}, e.g.
\code{PoS == "verb"}. Only rows where it is TRUE are looked up; all other rows
are kept, with \code{NA} in the added columns. \code{NA} in the condition counts as
FALSE.}

\item{multiple}{What to do when an id appears more than once in \code{lookup}.
\code{"first"} (default) keeps the first match and warns; \code{"all"} returns one
row per match, changing the row count; \code{"error"} stops.}

\item{fix_types}{Logical. If TRUE, character columns whose non-missing
values all parse as numbers are converted to numeric, with a message.
Default FALSE.}

\item{complete_only}{Logical. If TRUE, keep only rows with values on all of
the added columns. Default FALSE.}

\item{ignore_case}{Logical. If TRUE (default), match on lower-cased,
whitespace-trimmed ids.}

\item{quiet}{Logical. If TRUE, suppress the report. Default FALSE.}
}
\value{
\code{d} with the requested columns added. A data.table in, a data.table
out; otherwise a data.frame. Keys are not preserved.
}
\description{
Looks each id (\verb{id = }) up in a second dataset (\verb{lookup = }), matching on a
column in that dataset (\verb{lookup_id = }) and adds the requested columns
(\verb{lookup_cols = }).

The lookup can be a data frame already in R or a path to a file, and .csv,
.tsv, .txt, .rds, .xlsx and .xls are all read without issue. If
neither id column is named, the leftmost column of each dataset is used,
with a message saying so.

You can choose to simply add all columns by using \code{add_all = TRUE}.

\code{new_cols} renames the incoming columns, either by position or by name so
that only some are renamed. By position, give one name per column in
\code{lookup_cols}: \code{new_cols = c("freq", "len")}. By name, give only the ones to
rename, with the old name on the left: \code{new_cols = c(Log_Freq_HAL = "freq")}.

\code{add_subset} restricts the lookup to a subset of rows -- for example \code{PoS == "verb"} -- written as an ordinary expression. Rows outside the subset are
kept, with missing values in the new columns.

\code{fix_types = TRUE} converts incoming character columns whose values all
parse as numbers, reporting what it converted -- useful when the lookup has
been through a spreadsheet.

\code{complete_only = TRUE} keeps only rows with values on every incoming
column.
}
\examples{
\dontrun{
# both files use their leftmost column as the id
d2 <- add_values(d2, lookup = "ELP.csv",
                 lookup_cols = c("Log_Freq_HAL", "Length"))

# everything in the file
d2 <- add_values(d2, lookup = "ELP.csv", add_all = TRUE)

# explicit, renamed, verbs only
d2 <- add_values(d2, "Word", "ELP.csv", "Word",
                 c("Log_Freq_HAL", "Length"),
                 new_cols   = c("freq", "len"),
                 add_subset = PoS == "verb",
                 fix_types  = TRUE)
}

}
