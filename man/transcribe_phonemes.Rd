% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcribe_phonemes.R
\name{transcribe_phonemes}
\alias{transcribe_phonemes}
\title{Transcribe words to IPA, optionally coding phonemes}
\usage{
transcribe_phonemes(
  d,
  word_col = "word",
  dict = NULL,
  add_columns = FALSE,
  counts = TRUE,
  inventory = english_phonemes,
  ipa_col = "ipa",
  prefix = "ph_",
  quiet = FALSE,
  ...
)
}
\arguments{
\item{d}{A data.frame/data.table, or a plain character vector of words.}

\item{word_col}{Character: name of the word column in \code{d}. If \code{d} is a
character vector, this is used as the name of the resulting column.
Default \code{"word"}.}

\item{dict}{Pronunciation dictionary; see \code{word_to_ipa()}. If \code{NULL}
(default), \code{cmu_dict()} downloads and caches the CMU dictionary on first
use.}

\item{add_columns}{Logical. If FALSE (default), only the transcription
column is added. If TRUE, a column is also added for each phoneme,
containing the number of times it occurs in the word.}

\item{counts}{Logical. Only used when \code{add_columns = TRUE}. If TRUE
(default), phoneme columns hold counts; if FALSE, 0/1 presence.}

\item{inventory}{Character vector of phonemes to use as columns. Defaults to
\code{english_phonemes}, the full CMUdict inventory, so that every phoneme gets
a column whether or not it occurs in these particular words. Pass \code{NULL}
to use only the segments actually observed.}

\item{ipa_col}{Name of the transcription column to add. Default \code{"ipa"}.}

\item{prefix}{Prefix for the phoneme column names. Default \code{"ph_"}.}

\item{quiet}{Logical. If TRUE, suppress the coverage message. Default FALSE.}

\item{...}{Passed to \code{ipa_to_segments()} via \code{code_phonemes()}.}
}
\value{
\code{d} with a transcription column added, plus one column per phoneme
if \code{add_columns = TRUE}. Row order is preserved. A data.table in, a
data.table out; otherwise a data.frame.
}
\description{
Looks each word up in a pronunciation dictionary and adds an IPA
transcription column. Optionally also adds one column per phoneme.

\code{add_columns = TRUE} adds a column for each phoneme, holding either counts or
presence, set by \code{counts}. The default, \code{counts = TRUE}, records how many
times each phoneme occurs in the word; \code{counts = FALSE} records only whether
it occurs at all, as 0 or 1.

For most English word lists the two give nearly the same thing, because a
phoneme seldom occurs more than once in a short word. Presence is then the
simpler choice: a coefficient reads directly as the difference associated
with a word containing that sound. Counts are worth using when repetition is
itself of interest, or when the words are long enough for repeats to be
common. One mechanical consequence is worth knowing either way -- the counts
across a row sum to the number of phonemes in the word, so count columns
carry word length with them in a way presence columns largely do not, which
matters if length is also a predictor in the model.

The dictionary can be built with \code{stress_sensitive = TRUE}, which
distinguishes unstressed vowels -- schwa from its stressed counterpart --
rather than collapsing them. A different dictionary can be supplied
entirely, provided it has word and IPA columns.

The pieces are also available separately: \code{word_to_ipa()} for transcription
alone, \code{code_phonemes()} for coding IPA strings that came from somewhere
else, and \code{cmu_dict()} for the dictionary itself.
}
\examples{
\dontrun{
# transcription only
transcribe_phonemes(c("cat", "judge", "boat"))

# with phoneme counts
transcribe_phonemes(c("cat", "judge", "boat"), add_columns = TRUE)

# on an existing data frame, with columns that match across datasets
stimuli <- transcribe_phonemes(
  stimuli,
  word_col    = "Word",
  add_columns = TRUE,
  inventory   = english_phonemes
)
}

}
