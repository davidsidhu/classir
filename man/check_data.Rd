% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_data.R
\name{check_data}
\alias{check_data}
\title{Check a dataset for likely problems}
\usage{
check_data(
  d,
  missing_prop = 0.2,
  sd_cutoff = 5,
  rare_prop = 0.01,
  factor_max = Inf,
  unique_max = 5,
  sentinels = c(-9999, -999, -99, -9, 999, 9999),
  id = NULL,
  examples = 3,
  quiet = FALSE
)
}
\arguments{
\item{d}{A data.frame or data.table.}

\item{missing_prop}{Flag columns with at least this proportion missing.
Default 0.2.}

\item{sd_cutoff}{For numeric columns, flag values further than this many
standard deviations from the mean. Default 5. Set to \code{Inf} to skip.}

\item{rare_prop}{For text columns, flag values whose character pattern
occurs in fewer than this proportion of the column. Default 0.01.}

\item{factor_max}{Character columns with this many distinct values or fewer
are suggested as factors. Default \code{Inf}, so every character column is
suggested. Set a number to restrict it, or 0 to skip the check.}

\item{unique_max}{Numeric columns with this many distinct values or fewer
are flagged as possibly categorical. Default 5. Set to 0 to skip.}

\item{sentinels}{Numeric values commonly used as missing-value codes.
Flagged where they sit outside a column's range.
Default \code{c(-9999, -999, -99, -9, 999, 9999)}.}

\item{id}{Optional column name to check for duplicate identifiers.}

\item{examples}{How many example values to show per strange-cell flag.
Default 3.}

\item{quiet}{Logical. If TRUE, return the issues without printing.}
}
\value{
Invisibly, a list with \code{issues} (a data.frame of column, check, and
detail) and \code{suggestions} (paste-ready conversion code).
}
\description{
Scans every column for things that usually indicate a data problem: columns
stored as the wrong type, heavy missingness, constant columns, values that
look like missing-value placeholders, stray whitespace, case-only
duplicates, and strange cells -- individual values that don't look like the
rest of their column. Nothing is modified; suggested fixes are printed as
ready-to-paste code.

Every check here is a heuristic. A flag means "worth a look", not "wrong".
}
\section{Strange cells}{

For numeric columns this covers values far from the mean, non-whole numbers
in an otherwise whole-numbered column, and negatives in an otherwise
non-negative one. For text columns it compares each value's character
pattern -- letters, digits and punctuation, with runs collapsed -- against
the rest of the column, and flags patterns that are rare. A column of words
containing one entry with a digit in it, or one entry three times longer
than everything else, shows up here.
}

\examples{
\dontrun{
check_data(d2)
check_data(d2, id = "Word", sd_cutoff = 4)
}

}
