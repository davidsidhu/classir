% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep.R
\name{prep}
\alias{prep}
\title{Get a dataset ready to analyse}
\usage{
prep(
  d,
  complete = NULL,
  subset,
  scale = NULL,
  ref = NULL,
  effects = NULL,
  rename = NULL,
  keep = NULL,
  drop_others = FALSE,
  ask_conv = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{d}{A data.frame or data.table.}

\item{complete}{Character or numeric: columns that must have no missing
values. Rows missing any of them are dropped.}

\item{subset}{An unquoted expression selecting rows to keep, written the way
you would inside \code{d[...]}, e.g. \code{pos == "noun" & freq > 1}. Applied after
the missing-value cut and before anything else.}

\item{scale}{Character or numeric: numeric columns to standardise.}

\item{ref}{Named character vector: the reference level for each factor, e.g.
\code{c(PoS = "noun")}.}

\item{effects}{Character vector: two-level factors to effects code at
-0.5 / 0.5.}

\item{rename}{Named character vector, old name on the left, e.g.
\code{c(Log_Freq_HAL = "freq")}.}

\item{keep}{Character vector: extra columns to retain when
\code{drop_others = TRUE}.}

\item{drop_others}{Logical. If TRUE, keep only the columns named in the other
arguments. Default FALSE.}

\item{ask_conv}{Logical. If TRUE (default) and the session is interactive,
offer to convert character columns that hold only numbers.}

\item{quiet}{Logical. If TRUE, suppress the report. Default FALSE.}
}
\value{
The prepared data. A data.table in, a data.table out.
}
\description{
One function to prepare data for analysis. Rows with missing values on
identified columns are dropped first. Then categorical variables are prepped
by setting reference levels and effects coding. Then numeric variables are
standardised. Columns are then renamed.

\code{complete} names the columns that must have no missing values. Any row
missing one of them is dropped, so everything downstream works from the same
set of observations.

\code{subset} keeps only the rows you want, written as an ordinary expression --
\code{subset = pos == "noun" & freq > 1}. It is applied after the missing-value
cut, so any standardising happens on the rows that survive both.

\code{scale} names the numeric columns to standardise. This happens after the row
cut, so the means and SDs come from the rows that remain. Columns that were
effects coded are never standardised.

\code{ref} sets the reference level of a factor, written as
\code{ref = c(PoS = "noun")}. Several can be set at once.

\code{effects} names two-level factors to effects code, applying contrasts of
-0.5 and 0.5. The column stays a factor, so it still works with \code{emmeans} and
anything else that expects one.

\code{rename} renames columns, written as \code{rename = c(Log_Freq_HAL = "freq")},
with the old name on the left.

\code{drop_others = TRUE} keeps only the columns named in the other arguments,
plus any named in \code{keep}.
}
\details{
Character columns holding only numbers are detected and, in an interactive
session, listed so they can be converted on the spot. \code{ask_conv = FALSE}
turns the prompt off, which is what you want in a script.

A report of what was done is printed unless \code{quiet = TRUE}.
}
\examples{
\dontrun{
d <- prep(demo_words,
          complete = c("freq", "aoa", "concreteness"),
          subset   = pos != "adjective",
          scale    = c("freq", "aoa", "concreteness"),
          ref      = c(pos = "noun"),
          effects  = "sound_symbolic",
          rename   = c(concreteness = "conc"))
}

}
