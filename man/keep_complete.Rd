% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keep_complete.R
\name{keep_complete}
\alias{keep_complete}
\title{Keep complete cases on selected columns, with optional trimming and scaling}
\usage{
keep_complete(
  d,
  cols,
  id_col = NULL,
  drop_others = FALSE,
  scale_numeric = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{d}{A data.frame or data.table.}

\item{cols}{Character or numeric: the columns that must be complete.}

\item{id_col}{Optional character: an identifier column to retain when
\code{drop_others = TRUE}.}

\item{drop_others}{Logical. If TRUE, keep only \code{cols} and \code{id_col}. Default
FALSE.}

\item{scale_numeric}{Logical. If TRUE, standardize the numeric columns among
\code{cols} after the rows have been cut. Default FALSE.}

\item{quiet}{Logical. If TRUE, suppress the summary message. Default FALSE.}
}
\value{
The filtered data. A data.table in, a data.table out; otherwise a
data.frame.
}
\description{
Drops any row missing a value on selected columns.

\code{drop_others = TRUE} discards every column except the ones named, which is
useful when a wide dataset is being narrowed to the variables in an analysis.

\code{scale_numeric = TRUE} standardises the numeric columns among \code{cols}, and
does so after the rows have been cut, so the means and SDs come from the rows
that remain.

\code{drop_others = TRUE} discards every column that was not named, apart from an
identifier column named in \code{id_col}. This is the quickest way to reduce a
wide dataset to the variables an analysis actually uses, and it makes the
resulting object small enough to inspect.
}
\examples{
\dontrun{
# keep rows with values on all the predictors
d_use <- keep_complete(demo_words, c("freq", "aoa", "concreteness"))

# narrow to just those columns, keeping the word, and standardise them
d_use <- keep_complete(demo_words,
                       cols = c("freq", "aoa", "concreteness"),
                       id_col = "word",
                       drop_others = TRUE,
                       scale_numeric = TRUE)
}

}
