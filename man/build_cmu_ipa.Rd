% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcribe_phonemes.R
\name{build_cmu_ipa}
\alias{build_cmu_ipa}
\title{Build a word -> IPA lookup table from CMUdict}
\usage{
build_cmu_ipa(file, stress_sensitive = FALSE, quiet = FALSE)
}
\arguments{
\item{file}{Path to \code{cmudict.dict}.}

\item{stress_sensitive}{Logical. If TRUE, CMUdict's stress digits are used
to distinguish unstressed vowels: \code{AH0} maps to schwa and \code{ER0} to
r-coloured schwa, rather than collapsing with their stressed
counterparts. Default FALSE.}

\item{quiet}{Logical. If TRUE, suppress the summary message. Default FALSE.}
}
\value{
A data.frame with columns \code{word} (lower-cased) and \code{ipa}.
}
\description{
Parses a CMUdict file (\code{cmudict.dict}, from
\url{https://github.com/cmusphinx/cmudict}) and converts each ARPAbet
transcription to IPA. Alternate pronunciations -- the \code{word(2)}, \code{word(3)}
entries -- are dropped, keeping only the first pronunciation per word.
}
\details{
Most of the time you want \code{cmu_dict()} instead, which handles downloading
and caching. Use this directly only if you have your own copy of the file.
}
